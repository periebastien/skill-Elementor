# `post-info` — Information de la publication (Pro)

`x`+GROUPE = groupe de contrôles (sous-clés dans common-controls.md). Suffixes responsive `_tablet`/`_mobile` dispo sur les contrôles responsive.

## onglet content
- section_icon : `view`(traditional|inline)[=inline], `icon_list`:repeater[]

## onglet style
- section_icon_list : `space_between`:slider{unit,size}, `icon_align`(left|center|right), `divider`:'yes', `divider_style`(solid|double|dotted|dashed)[=solid], `divider_weight`:slider{unit,size}, `divider_width`:slider{unit,size}, `divider_height`:slider{unit,size}, `divider_color`:color[=#ddd]
- section_icon_style : `icon_color`:color, `icon_size`:slider{unit,size}
- section_text_style : `icon_typography_typography`+GROUPE typo, `text_indent`:slider{unit,size}, `text_color`:color
