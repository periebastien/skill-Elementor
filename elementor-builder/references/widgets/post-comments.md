# `post-comments` — Commentaires de publication (Pro)

`x`+GROUPE = groupe de contrôles (sous-clés dans common-controls.md). Suffixes responsive `_tablet`/`_mobile` dispo sur les contrôles responsive.

## onglet content
- section_content : `skin_temp`(), `source_type`(current_post|custom)[=current_post], `source_custom`
