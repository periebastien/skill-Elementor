# `woocommerce-product-rating` — Évaluation produit (Pro)

`x`+GROUPE = groupe de contrôles (sous-clés dans common-controls.md). Suffixes responsive `_tablet`/`_mobile` dispo sur les contrôles responsive.

## onglet style
- section_product_rating_style : `text_typography_typography`+GROUPE typo, `star_color`:color, `empty_star_color`:color, `link_color`:color, `star_size`:slider{unit,size}, `space_between`:slider{unit,size}, `alignment`(left|center|right|justify)
