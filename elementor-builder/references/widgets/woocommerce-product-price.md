# `woocommerce-product-price` — Prix produit (Pro)

`x`+GROUPE = groupe de contrôles (sous-clés dans common-controls.md). Suffixes responsive `_tablet`/`_mobile` dispo sur les contrôles responsive.

## onglet style
- section_price_style : `typography_typography`+GROUPE typo, `sale_price_typography_typography`+GROUPE typo, `text_align`(start|center|end), `price_color`:color, `sale_price_color`:color, `price_block`:'yes', `sale_price_spacing`:slider{unit,size}
