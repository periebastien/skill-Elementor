# `facebook-page` — Page Facebook (Pro)

`x`+GROUPE = groupe de contrôles (sous-clés dans common-controls.md). Suffixes responsive `_tablet`/`_mobile` dispo sur les contrôles responsive.

## onglet content
- section_content : `url`:txt, `tabs`(timeline|events|messages), `small_header`:'yes', `show_cover`:'yes'[=yes], `show_facepile`:'yes'[=yes], `show_cta`:'yes'[=yes], `height`:slider{unit,size}
