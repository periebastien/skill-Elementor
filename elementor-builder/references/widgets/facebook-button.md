# `facebook-button` — Bouton Facebook (Pro)

`x`+GROUPE = groupe de contrôles (sous-clés dans common-controls.md). Suffixes responsive `_tablet`/`_mobile` dispo sur les contrôles responsive.

## onglet content
- section_content : `type`(like|recommend)[=like], `layout`(standard|button|button_count|box_count)[=standard], `size`(small|large)[=small], `color_scheme`(light|dark)[=light], `show_share`:'yes', `show_faces`:'yes', `url_type`(current_page|custom)[=current_page], `url_format`(plain|pretty)[=plain], `url`:txt
