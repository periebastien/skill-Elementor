# `icon` — Icône (core)

`x`+GROUPE = groupe de contrôles (sous-clés dans common-controls.md). Suffixes responsive `_tablet`/`_mobile` dispo sur les contrôles responsive.

## onglet content
- section_icon : `selected_icon`:icons{value,library}, `view`(default|stacked|framed)[=default], `shape`(square|rounded|circle)[=circle], `link`:url{url,is_external}

## onglet style
- section_style_icon : `align`(start|center|end)[=center], `icon_colors`, `primary_color`:color, `secondary_color`:color, `hover_primary_color`:color, `hover_secondary_color`:color, `hover_animation`, `size`:slider{unit,size}, `fit_to_size`:'yes', `icon_padding`:slider{unit,size}, `rotate`:slider{unit,size}, `border_width`:dim{t,r,b,l,unit,isLinked}, `border_radius`:dim{t,r,b,l,unit,isLinked}
