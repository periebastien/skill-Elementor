# `facebook-embed` — Intégrer Facebook (Pro)

`x`+GROUPE = groupe de contrôles (sous-clés dans common-controls.md). Suffixes responsive `_tablet`/`_mobile` dispo sur les contrôles responsive.

## onglet content
- section_content : `type`(post|video|comment)[=post], `post_url`:txt, `video_url`:txt, `comment_url`:txt, `include_parent`:'yes', `show_text`:'yes', `video_allowfullscreen`:'yes', `video_autoplay`:'yes', `video_show_captions`:'yes'
