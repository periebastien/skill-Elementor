# `woocommerce-product-images` — Images produit (Pro)

`x`+GROUPE = groupe de contrôles (sous-clés dans common-controls.md). Suffixes responsive `_tablet`/`_mobile` dispo sur les contrôles responsive.

## onglet style
- section_product_gallery_style : `image_border_border`+GROUPE bordure, `thumbs_border_border`+GROUPE bordure, `sale_flash`:'yes'[=yes], `image_border_radius`:dim{t,r,b,l,unit,isLinked}, `spacing`:slider{unit,size}, `thumbs_border_radius`:dim{t,r,b,l,unit,isLinked}, `spacing_thumbs`:slider{unit,size}
