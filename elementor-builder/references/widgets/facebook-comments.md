# `facebook-comments` — Commentaires Facebook (Pro)

`x`+GROUPE = groupe de contrôles (sous-clés dans common-controls.md). Suffixes responsive `_tablet`/`_mobile` dispo sur les contrôles responsive.

## onglet content
- section_content : `comments_number`:nb[=10], `order_by`(social|reverse_time|time)[=social], `url_type`(current_page|custom)[=current_page], `url_format`(plain|pretty)[=plain], `url`:txt
