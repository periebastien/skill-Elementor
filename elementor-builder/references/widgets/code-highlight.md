# `code-highlight` — Mise en évidence du code (Pro)

`x`+GROUPE = groupe de contrôles (sous-clés dans common-controls.md). Suffixes responsive `_tablet`/`_mobile` dispo sur les contrôles responsive.

## onglet content
- section_content : `language`(markup|html|css|sass|scss|less|javascript|typescript|jsx|tsx|…)[=javascript], `code`:code, `line_numbers`:'yes'[=line-numbers], `copy_to_clipboard`:'yes', `highlight_lines`:txt, `word_wrap`:'yes', `theme`(default|dark|okaidia|solarizedlight|tomorrow|twilight)[=default], `height`:slider{unit,size}, `font_size`:slider{unit,size}
